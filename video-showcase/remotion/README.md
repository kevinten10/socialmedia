# Remotion Showcase

Uses the existing `video-generator` Remotion project as the React/TypeScript video generation baseline.

Render:

```powershell
.\video-showcase\remotion\render.ps1
```

Output:

```text
video-showcase/outputs/remotion-daily-sync.mp4
```
