# HyperFrames Showcase

Uses HyperFrames with HTML, CSS, and GSAP to render an agent-friendly video composition.

Render:

```powershell
.\video-showcase\hyperframes\render.ps1
```

Output:

```text
video-showcase/outputs/hyperframes-pipeline.mp4
```
