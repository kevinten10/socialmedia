# FFmpeg Showcase

Uses FFmpeg directly as the procedural editing and compositing baseline.

Render:

```powershell
.\video-showcase\ffmpeg\render.ps1
```

Output:

```text
video-showcase/outputs/ffmpeg-pipeline-card.mp4
```
